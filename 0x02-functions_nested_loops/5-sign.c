#include "main.h"

/**
 * main - Function checks for sign of a number
 * @n: argument used in the function
 * Return value: Always 0
 */
int add_sign(int n)
{
if (n > 0)
{
_putchar('+');
return (1);
}
else if (n < )
{
_putchar('-');
return (-1);
}
else 
{
_putchar('0');
return (0);
}
}