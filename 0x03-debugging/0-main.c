#include "main.h"
/**
 * main - tests for a function that prints if a number is positive or 
 *          negative.
 * Return value: o
 */
int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}