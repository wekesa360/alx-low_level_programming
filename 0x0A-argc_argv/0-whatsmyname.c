#include "holberton.h"
#include <stdio.h>

/**
 * main - Prints its name
 * @argc: argument count
 * @argv: argument vector
 * 
 * Return: always 0
 * 
 */

int main(int argc, char *argv[0]){

    (void) argc;
        printf("%s\n", argv[0]);

        return 0;
}