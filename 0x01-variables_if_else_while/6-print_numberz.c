#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints numbers 0 to 9 in ascending order
 * 
 * return value: Always 0
 */  
int main (void)
{
for (int i; i <= 9; i++)
putchar(i);
putchar('\n');
return (0);
}